// Common transition settings
export const transitions = {
  default: {
    duration: 0.5,
    ease: "easeOut"
  },
  fast: {
    duration: 0.2,
    ease: "easeOut"
  },
  slow: {
    duration: 0.8,
    ease: "easeInOut"
  }
};

// Common animation delays
export const delays = {
  immediate: 0,
  short: 0.2,
  medium: 0.4,
  long: 0.6
};

// Common animation scales
export const scales = {
  hover: 1.02,
  tap: 0.98,
  normal: 1
};

// Common animation distances
export const distances = {
  small: 5,
  medium: 10,
  large: 20
};
