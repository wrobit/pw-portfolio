const home = "/";
const work = "/work";
const about = "/about";
const contact = "/contact";
const project = "/project";
const projects = "/projects";
const error404 = "/404";

export const routes = {
  home,
  about,
  work,
  projects,
  contact,
  project,
  error404
};
